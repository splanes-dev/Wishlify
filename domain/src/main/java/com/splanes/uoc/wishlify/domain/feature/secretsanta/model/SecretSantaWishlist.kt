package com.splanes.uoc.wishlify.domain.feature.secretsanta.model

/** Wishlist shared within a Secret Santa event. */
data class SecretSantaWishlist(
  val wishlistId: String,
  val title: String,
)
