package com.splanes.uoc.wishlify.presentation.feature.shared.feature.list.model

/**
 * Available actions exposed from the shared wishlists list settings UI.
 */
enum class SharedWishlistsSettings {
  Search,
  Filter,
}
