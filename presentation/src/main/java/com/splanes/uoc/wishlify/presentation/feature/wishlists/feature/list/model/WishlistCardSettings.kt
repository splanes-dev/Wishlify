package com.splanes.uoc.wishlify.presentation.feature.wishlists.feature.list.model

/**
 * Available actions exposed from an individual wishlist card.
 */
enum class WishlistCardSettings {
  FilterProducts,
  BackToPrivate,
  Edit,
  Share,
  Info,
  Delete,
}
