package com.splanes.uoc.wishlify.presentation.feature.wishlists.feature.detail.shared.model

/**
 * Available actions exposed from the own shared wishlist detail settings UI.
 */
enum class SharedOwnWishlistSettings {
  Filter,
  BackToPrivates,
  Info
}
