package com.splanes.uoc.wishlify.presentation.feature.shared.feature.detail.model

/**
 * Available actions exposed from the shared wishlist detail settings UI.
 */
enum class SharedWishlistSettings {
  Filter,
  Info
}
