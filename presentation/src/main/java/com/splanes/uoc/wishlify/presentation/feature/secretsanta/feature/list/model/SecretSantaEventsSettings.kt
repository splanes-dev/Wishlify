package com.splanes.uoc.wishlify.presentation.feature.secretsanta.feature.list.model

/**
 * Available actions exposed from the Secret Santa events list settings UI.
 */
enum class SecretSantaEventsSettings {
  Search,
}
